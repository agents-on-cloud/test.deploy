pipeline {
    agent any
    stages {
        stage('Display tag name') {
            steps {
                echo "Tag name: ${env.GIT_TAG}"
            }
        }
        // Add more stages as needed for testing, deployment, etc.
    }
}